# Habit Tracker (Web)

A clean, useful habit tracker that runs entirely in the browser.

## Features
- Add / delete habits
- Mark as done for today
- Automatic streak calculation
- Stats (total habits + done today)
- Data saved in `localStorage` (persists between visits)
- No backend, no install, no account

## How to use
Just open `index.html` in any modern browser.
